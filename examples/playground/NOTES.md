# Playground notes

A scratch project for trying out the agent's builtin tools. It contains:

- `calc.py` — basic arithmetic (with one deliberate bug in `subtract`).
- `inventory.py` — sample data plus `total_value()` / `out_of_stock()` helpers.

## Open TODOs

- TODO: add a reorder-threshold warning to `inventory.out_of_stock`.
- TODO: write unit tests for `calc.divide`'s divide-by-zero path.

Search this repo for the string `TODO` to find work in progress.
